# Postgres DSN. Matches what `docker compose up` exposes locally.
DATABASE_URL=postgresql+psycopg://zeff:zeff@localhost:5432/zeff

# Standard Python log level (DEBUG, INFO, WARNING, ERROR).
LOG_LEVEL=INFO
